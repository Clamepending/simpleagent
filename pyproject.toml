[project]
name = "adminagent"
version = "0.1.0"
description = "Minimal webhook gateway for forwarding events"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "flask",
    "requests",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
